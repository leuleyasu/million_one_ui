import 'package:flutter/material.dart';

class name4 extends StatelessWidget {
  const name4({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Column(
      children: [
        Text('search Page',
            style: TextStyle(fontSize: 35, fontWeight: FontWeight.bold)),
      ],
    ));
  }
}
